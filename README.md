# Retirement Planner Streamlit App

A local SQLite-backed retirement planning dashboard for scenario modeling.

## Run

```bash
pip install -r requirements.txt
streamlit run app.py
The app stores local data in retirement_planner.db, which is intentionally ignored by git.
